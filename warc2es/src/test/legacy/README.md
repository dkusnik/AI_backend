# Legacy Shell Tests (Archived)

These scripts were moved from `src/test/` during prerelease cleanup (`T-262`).
They are not wired into `test-cli` and are kept only for historical reference.

- `test-merge-synthetic-s4-collision.sh`
- `test-merge-synthetic-s5-url-migration.sh`
